# --- Node / Vite (Frontend) ---
node_modules/
dist/
dist-ssr/
*.local
.eslintcache

# --- Rust / Cargo (prana_engine) ---
target/
**/*.rs.bk
# Note: Keep Cargo.lock if this is an app, ignore if it's a library
# Cargo.lock

# --- WASM Build Artifacts (from your screenshot) ---
# This catches the .wasm, .d, and json files generated by wasm-pack/rustc
prana_engine/pkg/
*.wasm
*.d
*.timestamp
.rustc_info.json
CACHEDIR.TAG
.cargo-lock
dep-lib-*
invoked.timestamp
lib-prana_engine*

# --- IDE & OS ---
.DS_Store
.vscode/
.idea/
*.swp
prana-os/public/video/*.mp4
prana-os/public/audio/*

prana-os/public/video/nn/*.mp4
prana-os/.env
public/video/*.mp4
/public/audio/*
.env
.vite/
public/video/*.webm
